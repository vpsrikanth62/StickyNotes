<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (not really used since no launcher activity) -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#7C3AED</item>
        <item name="colorPrimaryDark">#0D0F1C</item>
        <item name="colorAccent">#EC4899</item>
    </style>

    <!--
        Edit Note Activity Theme:
        - Translucent so the home screen bleeds through
        - On API 31+ we add blur-behind in code
        - Dialog-style animations
    -->
    <style name="EditNoteTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">#7C3AED</item>
        <item name="colorPrimaryDark">#0D0F1C</item>
        <item name="colorAccent">#EC4899</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/EditorWindowAnimation</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#00000000</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Smooth scale-up animation for the editor -->
    <style name="EditorWindowAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_up_fade_in</item>
        <item name="android:activityOpenExitAnimation">@android:anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_down_fade_out</item>
    </style>

    <!-- Motivation overlay: same as editor but smaller blur -->
    <style name="MotivationTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/EditorWindowAnimation</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#00000000</item>
    </style>

</resources>
