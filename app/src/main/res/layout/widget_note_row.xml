<?xml version="1.0" encoding="utf-8"?>
<!--
  Checkbox left edge lines up with title/progress (no extra start padding on the row).
  End padding matches for a balanced chip.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="34dp"
    android:orientation="horizontal"
    android:gravity="top"
    android:background="@drawable/bg_widget_row_chip"
    android:paddingStart="@dimen/widget_row_h_pad"
    android:paddingEnd="@dimen/widget_row_h_pad"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <TextView
        android:id="@+id/tv_item_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="top"
        android:minHeight="0dp"
        android:minLines="1"
        android:maxLines="12"
        android:gravity="start"
        android:textColor="#F2FFFFFF"
        android:textSize="12.5sp"
        android:fontFamily="sans-serif"
        android:letterSpacing="0.01"
        android:includeFontPadding="false"
        android:lineSpacingExtra="1dp"
        android:ellipsize="end" />

</LinearLayout>
