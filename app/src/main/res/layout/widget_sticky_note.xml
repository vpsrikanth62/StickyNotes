<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_widget_glass"
    android:paddingStart="@dimen/widget_inner_padding"
    android:paddingEnd="@dimen/widget_inner_padding"
    android:paddingBottom="8dp">

    <!-- Header: single horizontal row so title + edit align predictably -->
    <LinearLayout
        android:id="@+id/header_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="38dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="6dp"
        android:paddingBottom="4dp">

        <LinearLayout
            android:id="@+id/title_block"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="start">

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/widget_title"
                android:textColor="#FFFFFFFF"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.02"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tv_progress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="@string/widget_progress_empty"
                android:textColor="#8AFFFFFF"
                android:textSize="9sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <ImageView
            android:id="@+id/btn_widget_edit"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:padding="6dp"
            android:src="@drawable/ic_widget_edit"
            android:background="@drawable/bg_widget_edit_btn"
            android:scaleType="centerInside"
            android:contentDescription="@string/widget_edit" />
    </LinearLayout>

    <TextView
        android:id="@+id/divider_header"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/header_row"
        android:background="@drawable/bg_divider_glass"
        android:text="" />

    <FrameLayout
        android:id="@+id/list_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/divider_header"
        android:layout_above="@id/widget_footer_hint"
        android:minHeight="32dp">

        <TextView
            android:id="@+id/lv_notes"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:text="" />

        <!-- OEM-safe fallback for launchers that fail collection widget rendering. -->
        <LinearLayout
            android:id="@+id/compat_rows"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone"
            android:paddingTop="4dp"
            android:paddingBottom="2dp">

            <TextView
                android:id="@+id/tv_row_1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="5dp"
                android:minHeight="34dp"
                android:background="@drawable/bg_widget_row_chip"
                android:paddingStart="@dimen/widget_row_h_pad"
                android:paddingEnd="@dimen/widget_row_h_pad"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:textColor="#F2FFFFFF"
                android:textSize="12.5sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:maxLines="4"
                android:ellipsize="end"
                android:gravity="start|center_vertical" />

            <TextView
                android:id="@+id/tv_row_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="5dp"
                android:minHeight="34dp"
                android:background="@drawable/bg_widget_row_chip"
                android:paddingStart="@dimen/widget_row_h_pad"
                android:paddingEnd="@dimen/widget_row_h_pad"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:textColor="#F2FFFFFF"
                android:textSize="12.5sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:maxLines="4"
                android:ellipsize="end"
                android:gravity="start|center_vertical" />

            <TextView
                android:id="@+id/tv_row_3"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="5dp"
                android:minHeight="34dp"
                android:background="@drawable/bg_widget_row_chip"
                android:paddingStart="@dimen/widget_row_h_pad"
                android:paddingEnd="@dimen/widget_row_h_pad"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:textColor="#F2FFFFFF"
                android:textSize="12.5sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:maxLines="4"
                android:ellipsize="end"
                android:gravity="start|center_vertical" />

            <TextView
                android:id="@+id/tv_row_4"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="34dp"
                android:background="@drawable/bg_widget_row_chip"
                android:paddingStart="@dimen/widget_row_h_pad"
                android:paddingEnd="@dimen/widget_row_h_pad"
                android:paddingTop="6dp"
                android:paddingBottom="6dp"
                android:textColor="#F2FFFFFF"
                android:textSize="12.5sp"
                android:fontFamily="sans-serif"
                android:includeFontPadding="false"
                android:maxLines="4"
                android:ellipsize="end"
                android:gravity="start|center_vertical" />
        </LinearLayout>

        <!-- Same horizontal bounds as list (no extra inset — was causing misalignment) -->
        <LinearLayout
            android:id="@+id/tv_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingStart="0dp"
            android:paddingEnd="0dp">

            <TextView
                android:id="@+id/tv_empty_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_empty_title"
                android:textColor="#99FFFFFF"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:includeFontPadding="false" />

            <TextView
                android:id="@+id/tv_empty_sub"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@string/widget_empty_sub"
                android:textColor="#55FFFFFF"
                android:textSize="10sp"
                android:fontFamily="sans-serif"
                android:gravity="center"
                android:includeFontPadding="false" />
        </LinearLayout>
    </FrameLayout>

    <TextView
        android:id="@+id/widget_footer_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:paddingTop="2dp"
        android:text="@string/widget_footer_hint"
        android:textColor="#3AFFFFFF"
        android:textSize="8sp"
        android:fontFamily="sans-serif"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:visibility="gone" />

</RelativeLayout>
